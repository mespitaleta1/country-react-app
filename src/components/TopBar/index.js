import React from "react";

const SearchBar = () => {
  return <p>Search Bar</p>;
};

const FilterBar = () => {
  return <p>Filter</p>;
};

const TopBar = () => {
  return (
    <>
      <SearchBar />
      <FilterBar />
    </>
  );
};
export default TopBar;
